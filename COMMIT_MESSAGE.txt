feat: Implement AsNoTracking/AsTracking optimization for EF Core

## Changes

### Core Infrastructure
- Add `FindByIdForUpdateAsync` method to `IRepositoryBaseAsync` interface
- Implement `AsNoTracking()` for all read-only operations in `EfRepositoryBase`:
  - FindByIdAsync, FindOneAsync, FindAsync
  - GetAllAsync, ProjectBy
  - GetByPageFilter (all overloads)
- Implement `AsTracking()` for write operations:
  - FindByIdForUpdateAsync (new method)
  - DeleteByIdAsync

### Application Layer Updates
Update all Command Handlers to use `FindByIdForUpdateAsync`:
- Resource Service: 19 Update handlers
- Identity Service: 2 Update handlers  
- Order Service: 4 Update handlers

### Bug Fixes
- Fix DebugController property names (Order → OrderIndex, SectionId → CourseId)

### Cleanup
- Remove temporary documentation and scripts
- Add implementation summary documentation

## Benefits
- Improved read performance with AsNoTracking
- Proper change tracking for updates with AsTracking
- Clear intent separation between read and write operations
- Reduced memory overhead for queries
- Prevention of tracking conflicts

## Breaking Changes
None - All changes are internal implementation details

## Testing
- ✅ Build successful
- ✅ No compile errors
- ✅ All handlers updated consistently
